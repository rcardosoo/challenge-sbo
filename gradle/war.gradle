apply plugin: "war"

bootWar {
    mainClass = "com.triade.simple.SimpleApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
